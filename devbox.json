{
  "packages": [
    "nix",
    "gum",
    "sops",
    "age",
    "git",
    "jq",
    "bats"
  ],
  "shell": {
    "init_hook": [
      "echo '☕ Welcome to the Cafaye OS development shell!'"
    ],
    "scripts": {
      "check": "./bin/test.sh",
      "test": "./bin/test.sh",
      "test-full": "docker build --platform linux/amd64 -t cafaye-factory . && docker run --platform linux/amd64 --rm -it cafaye-factory",
      "vm": "nix run --extra-experimental-features 'nix-command flakes' .#debug-vm",
      "check-ci": "nix flake check --extra-experimental-features 'nix-command flakes'"
    }
  }
}