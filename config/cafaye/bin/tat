#!/usr/bin/env bash
set -euo pipefail

session="${1:-cafaye}"

if ! command -v tmux >/dev/null 2>&1; then
  echo "tmux is required for tat." >&2
  exit 1
fi

if tmux has-session -t "$session" 2>/dev/null; then
  exec tmux attach -t "$session"
else
  exec tmux new -s "$session"
fi
